main() {
  print("welcome to dart");
  // final String name;
  // name="riya";
  // print(name);
  final names = ["riya", "kaya", "siya"];
  // names=["sitya","tiya","tanya"];
  // the final keyword is used to declare variables that can be assigned only once but are initialized at runtime (not at compile-time).
  // IN FINAL WE CAN CHANGE DATA BUT IN CONST WE CANNOT CHANGE IN DATA
}
